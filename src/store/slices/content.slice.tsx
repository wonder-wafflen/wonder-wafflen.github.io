import { createSlice } from "@reduxjs/toolkit";

const initialState = {
};

// todo finish Store and this slice.
const contentSlice = createSlice({
  name: "content",
  initialState,
  reducers: {
  },
});

export const {  } = contentSlice.actions;

export default contentSlice.reducer;
